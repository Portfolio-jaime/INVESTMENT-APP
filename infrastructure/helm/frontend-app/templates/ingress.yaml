{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "frontend-app.fullname" . }}
  labels:
    {{- include "frontend-app.labels" . | nindent 4 }}
    component: frontend
    landing-page: {{ .Values.landingPage.enabled | quote }}
  {{- with .Values.ingress.annotations }}
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/use-regex: "true"
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          # API proxy routes for backend services
          - path: /api/portfolio(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: portfolio-manager
                port:
                  number: 8003
          - path: /api/market-data(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: market-data
                port:
                  number: 8001
          - path: /api/analysis(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: analysis-engine
                port:
                  number: 8002
          - path: /api/ml-prediction(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: ml-prediction
                port:
                  number: 8004
          # Frontend application (must be last for catch-all)
          {{- range .paths }}
          - path: {{ .path }}
            {{- if .pathType }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              service:
                name: {{ include "frontend-app.fullname" $ }}
                port:
                  number: {{ $.Values.service.port }}
          {{- end }}
    {{- end }}
{{- end }}