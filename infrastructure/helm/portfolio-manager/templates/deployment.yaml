apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "portfolio-manager.fullname" . }}
  labels:
    {{- include "portfolio-manager.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "portfolio-manager.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "portfolio-manager.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.config.apiPort }}
              protocol: TCP
          env:
            - name: COLOMBIAN_MARKETS
              value: "{{ .Values.config.colombianMarkets }}"
            - name: API_PORT
              value: "{{ .Values.config.apiPort }}"
            - name: DB_HOST
              value: "{{ .Values.config.timescaleHost }}"
            - name: DB_USER
              value: "trii_user"
            - name: DB_PASSWORD
              value: "trii_password"
            - name: DB_NAME
              value: "trii_dev"
            - name: REDIS_HOST
              value: "{{ .Values.config.redisHost }}"
            - name: TIMESCALE_HOST
              value: "{{ .Values.config.timescaleHost }}"
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5