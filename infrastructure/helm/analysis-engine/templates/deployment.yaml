apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "analysis-engine.fullname" . }}
  labels:
    {{- include "analysis-engine.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "analysis-engine.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "analysis-engine.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.config.apiPort }}
              protocol: TCP
          env:
            - name: COLOMBIAN_MARKETS
              value: "{{ .Values.config.colombianMarkets }}"
            - name: API_PORT
              value: "{{ .Values.config.apiPort }}"
            - name: REDIS_HOST
              value: "{{ .Values.config.redisHost }}"
            - name: REDIS_URL
              value: "redis://{{ .Values.config.redisHost }}:6379"
            - name: MARKET_DATA_SERVICE_URL
              value: "http://market-data:8001"
            - name: DEBUG
              value: "true"
            - name: CACHE_ENABLED
              value: "true"
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 180
            periodSeconds: 30
            timeoutSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 120
            periodSeconds: 15
            timeoutSeconds: 5