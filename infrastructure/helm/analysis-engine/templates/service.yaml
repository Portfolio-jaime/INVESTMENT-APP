apiVersion: v1
kind: Service
metadata:
  name: {{ include "analysis-engine.fullname" . }}
  labels:
    {{- include "analysis-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "analysis-engine.selectorLabels" . | nindent 4 }}