apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: trii-dev

resources:
  - service.yaml
  - deployment.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: market-data
  app.kubernetes.io/component: backend-service
  app.kubernetes.io/part-of: trii-platform
  app.kubernetes.io/managed-by: argocd
  tier: core-services

# Sync wave 1 - Core service that others depend on
commonAnnotations:
  argocd.argoproj.io/sync-wave: "1"
