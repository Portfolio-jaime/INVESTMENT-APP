apiVersion: v1
kind: Service
metadata:
  name: market-data-service
  namespace: trii-dev
  labels:
    app: market-data
    app.kubernetes.io/name: market-data
    app.kubernetes.io/component: service
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app: market-data
