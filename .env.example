# GitHub Actions Secrets Configuration
# Copy this template to configure your GitHub repository secrets

# Docker Hub authentication (required for pushing images)
DOCKERHUB_USERNAME=your-dockerhub-username
DOCKERHUB_PASSWORD=your-dockerhub-access-token

# Infrastructure configuration
POSTGRES_PASSWORD=your-postgres-password
RABBITMQ_DEFAULT_USER=your-rabbitmq-user
RABBITMQ_DEFAULT_PASS=your-rabbitmq-password

# Application configuration
VITE_API_URL=https://api.trii-platform.com
VITE_APP_VERSION=1.0.0

# ArgoCD configuration (optional)
ARGOCD_SERVER=your-argocd-server
ARGOCD_TOKEN=your-argocd-token